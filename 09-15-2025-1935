//Finds out if the geiven letters are present in the text stream and returns the number of words where it is not present.

int canBeTypedWords(string text, string brokenLetters) {
        bool tippie = true;
        int count = 0;
        for (int i = 0; i < text.length(); i++) {
            if (text[i] == ' ')
                (tippie) ? (count++) : (tippie = true);
            else {
                for (int j = 0; j < brokenLetters.length(); j++) {
                    if (text[i] == brokenLetters[j]) {
                        tippie = false;
                    }
                }
            }
        }
    return tippie ? count+1 : count;
    }
